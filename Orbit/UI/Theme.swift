import SwiftUI

enum Theme {
    static let background = Color(red: 0.027, green: 0.031, blue: 0.078)
    static let card = Color(white: 1).opacity(0.06)
    static let gold = Color(red: 1.0, green: 0.84, blue: 0.42)
    static let textDim = Color.white.opacity(0.45)
}
